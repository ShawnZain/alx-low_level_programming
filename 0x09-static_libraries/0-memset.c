/**
 * _memset - replaces specific bytes from one string with another
 * @s: string to change
 * @b: what to copy to s
 * @n: the aount of bytes to copy
 *
 * Return: changed array with new value
 */

char *_memset(char *s, char b, unsigned int n);
