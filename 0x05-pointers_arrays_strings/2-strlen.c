/**
 * _strleng - finds the length of a string
 * @s - string whose length to check for
 * Return: length of the string
 */

int _strlen(char *s)
{
	int i, l;

	for (i = 1; s[i-1] != '\0'; i++)
	{
		l = i;
	}

	return (l + 1);
}
